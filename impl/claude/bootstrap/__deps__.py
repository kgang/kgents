"""
Module dependency manifest for bootstrap.

Bootstrap is the irreducible kernel - it has no internal dependencies.
"""

# kgents modules this module depends on (none - bootstrap is foundational)
INTERNAL_DEPS: list[str] = []

# External pip packages required at runtime
PIP_DEPS: list[str] = []
