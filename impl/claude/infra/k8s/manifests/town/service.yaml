# Agent Town Service
#
# Exposes town-service deployment within the cluster.
#
# See: plans/velvety-mapping-puddle.md Phase 5
---
apiVersion: v1
kind: Service
metadata:
  name: town-service
  namespace: kgents-triad
  labels:
    app.kubernetes.io/name: town-service
    app.kubernetes.io/component: simulation
    app.kubernetes.io/part-of: kgents
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: town-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
