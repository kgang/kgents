/**
 * DirectorDashboard - Re-export from parent components directory
 *
 * The canonical "Documents" viewer for kgents.
 * The actual implementation is in ../DirectorDashboard.tsx
 *
 * Philosophy:
 *   "The canvas breathes. Documents arrive, analyze, become ready."
 */

export { DirectorDashboard } from '../DirectorDashboard';
export type { DirectorDashboardProps } from '../DirectorDashboard';
