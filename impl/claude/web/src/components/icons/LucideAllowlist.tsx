/**
 * LUCIDE ICON ALLOWLIST
 *
 * Curated subset of Lucide icons for kgents.
 *
 * Philosophy:
 * - Use glyphs first, Lucide as fallback
 * - Only essential UI patterns (navigation, files, actions)
 * - Tasteful > feature-complete
 *
 * Usage:
 *   import { ArrowLeft, Check } from '@/components/icons/LucideAllowlist';
 *   <ArrowLeft size={16} />
 */

// Navigation
export {
  ArrowLeft,
  ArrowRight,
  ArrowUp,
  ArrowDown,
  ChevronDown,
  ChevronRight,
  ChevronUp,
  ChevronLeft,
} from 'lucide-react';

// Files
export {
  File,
  FileText,
  Folder,
  FolderOpen,
} from 'lucide-react';

// Actions
export {
  Save,
  Search,
  Settings,
  Filter,
  Plus,
  Minus,
  X,
  Check,
} from 'lucide-react';

// Graph
export {
  GitBranch,
  Network,
  Link,
  Unlink,
} from 'lucide-react';

// Witness
export {
  Eye,
  EyeOff,
  Sparkles,
  Crosshair,
} from 'lucide-react';

// UI
export {
  Menu,
  MoreHorizontal,
  MoreVertical,
  Maximize2,
  Minimize2,
} from 'lucide-react';
