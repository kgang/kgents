# Identity Section Configuration
# This YAML template defines metadata for the identity section.
# The actual content is compiled from code in Wave 2+.

name: identity
description: Project identity, philosophy, and current focus
required: true
phases: []  # All phases

# Token budget target
token_budget: 600

# Source files for cache invalidation
sources:
  - CLAUDE.md

# Content structure (for reference, compiled from code)
structure:
  - project_name: "kgents - Kent's Agents"
  - tagline: "A specification for tasteful, curated, ethical, joy-inducing agents."
  - philosophy:
      - specification_first
      - python_cpython_model
      - alphabetical_taxonomy
      - agentese_meta_protocol
  - focus: "Deeply specifying and implementing the agent ecosystem"
  - taxonomy_table: true  # Include the agent taxonomy table
