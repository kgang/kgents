# Ghost files are ephemeral - generated by the sensorium
# Do not commit actual ghost files, only this .gitignore

# Test flinches (algedonic signals)
test_flinches.jsonl

# CI signals
ci_signals.jsonl

# Future ghost files
thought_stream.md
tension_map.json
shadow.patch
health.status
next_step.py
